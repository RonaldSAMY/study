# Copy to `.env` and fill in your values:  cp .env.example .env

# The domain (or subdomain) that will serve this site.
# Its DNS A/AAAA record must point at this server's public IP.
DOMAIN=maths.example.com

# Email used by Let's Encrypt for certificate notices.
ACME_EMAIL=you@example.com
